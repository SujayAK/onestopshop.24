{
  "name": "onestop",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 5175",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:worker": "npx wrangler deploy --config cloudflare/wrangler.toml",
    "deploy:worker:prod": "npx wrangler deploy --config cloudflare/wrangler.toml --env production"
  },
  "devDependencies": {
    "prettier": "^3.3.1",
    "vite": "^6.0.0",
    "wrangler": "^4.81.1"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "three": "^0.176.0"
  }
}
